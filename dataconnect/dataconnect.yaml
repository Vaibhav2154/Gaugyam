specVersion: "v1beta"
serviceId: "gaugyam"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gaugyam-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
